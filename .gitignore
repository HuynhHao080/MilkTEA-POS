# Visual Studio user settings - Mỗi người dùng setting riêng
.vs/
*.user
*.suo

# Test results - Không cần track
TestResults/

# Temporary files
*.log
*.cache

# NOTE: bin/ and obj/ are now tracked by Git (removed from ignore list)
# This ensures build outputs (.exe, .dll) are included in pull/push
